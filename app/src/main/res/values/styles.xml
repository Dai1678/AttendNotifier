<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorDarkBrown</item>
        <item name="colorPrimaryDark">@color/colorBrown</item>
        <item name="android:statusBarColor">@color/colorGrayBrown</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="AppTheme.BottomAppBar" parent="Widget.MaterialComponents.BottomAppBar" />

</resources>
